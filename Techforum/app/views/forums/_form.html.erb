<br />
<%= form_for(emp_logins_forum_path) do |f| %>
  <% if current_emp_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
      <% @forum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
<table align='center' cellspacing='10px' cellpadding='5px' border='0px'>
<tr><td>Qid:</td><td><input type='text' name='forum[qid]' /></td></tr>
<tr><td>Question:</td><td><input type='text' name='forum[question]' /></td></tr>
<tr><td>Type:</td><td><%= select_tag('forum[type]', options_for_select(%w[Challenge Issue Question])) %></td></tr>
<tr><td>Technology:</td><td></td></tr>
<tr><td>Employee Id:</td><td><input type='text' value= '<%= current_emp_login.emp_master.tcs_id %>'></td></tr>
</table>
<br />
<div class="actions"><%= f.submit "Apply" %>
<% end %>
